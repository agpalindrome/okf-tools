# Vale falsification probe

Temporary. This file exists to make the `prose` check fail on purpose, so a
green check can be believed. Delete it with the branch.

The next sentence carries two deliberate errors: the behaviors it describes
were not standardised anywhere.
